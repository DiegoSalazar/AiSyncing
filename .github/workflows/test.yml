name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck
        run: shellcheck setup.sh sync.sh uninstall.sh tests/run.sh

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure git
        run: |
          git config --global user.name "CI"
          git config --global user.email "ci@test.com"
          git config --global init.defaultBranch main

      - name: Run tests
        run: bash tests/run.sh
